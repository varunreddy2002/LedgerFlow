# OCR logic is being moved to the ingestion agent (app/application/agents/)
# This stub exists to avoid import errors during transition.


def process_pdf_document(document_id: int, business_id: int) -> None:
    """Placeholder — will be replaced by the ingestion agent graph."""
    raise NotImplementedError("OCR is handled by the ingestion agent.")
